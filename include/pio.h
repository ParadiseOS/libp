#ifndef PIO_H_
#define PIO_H_

// Prints n characters of a string to the terminal
void putn(const char* string, int length);

// Prints a null terminated string to the terminal
void puts(const char* str);

#endif // PIO_H_