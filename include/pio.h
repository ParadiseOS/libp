#ifndef PIO_H_
#define PIO_H_

// todo: have fun
void pprintf();

// Prints n characters of a string to the terminal
void pputn(const char *string, int length);

// Prints a null terminated string to the terminal
void pputs(const char *str);

#endif // PIO_H_